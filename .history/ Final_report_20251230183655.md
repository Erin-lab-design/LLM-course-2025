# Week1 What are tokenizers?

Tokenizers are pre-processing tools used in neural text(or other modalities) processing to convert raw text into a sequence of numerical identifiers (IDs) that a neural network can process.
In the era of LLMs, tokenization is synonymous with sub-word algorithms, as they strike the balance between the characters granularity and the semantic meaning of words.

##### Why are they important for language modeling and LLMs?

Fundamentally, neural networks can only operate on numerial vectors (matrices), not symbolic text. Tokenizers serve as the brige between human language texts and numerical IDs.
Handling Unknown Words (OOV): Traditional word-level models can't deal with "Out-Of-Vocabulary" words. Modern tokenizers on the contrary can easily break down rare or unknown words into known subword parts(prefixes, suffixes), allowing the model be more flexible in processing any text string.
Vocabulary Efficiency: They maintain a manageable vocabulary size (e.g., 32k to 100k tokens) while still being able to do text processing and generation tasks(e.g., translation task). This balances computational requirements with expressiveness.
Sequence Length: Compared to character-level models, subword tokenizers produce shorter sequences, which is crucial because the computational cost of Transformers (Attention mechanism) grows quadratically with sequence length.

##### What different tokenization algorithms there are and which ones are the most popular ones and why?

The algorithms being used in tokenization are:

1. Byte Pair Encoding (BPE)
It iteratively merges the most frequent pair of adjacent symbols (characters or character sequences) into a new, single symbol. This process allows the model to build a vocabulary of frequent subword units. It's the most popular one because of its superiority in morphological awareness and efficiency, which enables it to betterly solve the OOV Problem, and transliterate names between alphabets by copying or adapting subword units based on phonological similarity.
2. SentencePiece
SentencePiece itself refers more to a tools libray/system than to a certain algorithm, but we can still view it as a complete and mature working flow of tokenization.
Unlike traditional tokenizers that rely on pre-tokenization (like splitting by spaces, which is hard for languages like Chinese or Japanese), SentencePiece treats the input as a raw data stream (including spaces as a special character like _). It is language-independent and implements algorithms like BPE and Unigram.
It's popular to the extend that it almost became the industry standard of LLM today because of its robustness of handling noisy(e.g. oov), multilingual nature of the dataset for LLM training.
3. Alternative & Baseline Approaches
Word-Level Tokenization:
Method: Uses a fixed vocabulary of the most frequent 30,000–50,000 words.
Drawbacks: It cannot translate words not in its vocabulary (Out-Of-Vocabulary or OOV words). It often requires a "back-off" dictionary to look up rare words, which is complex and often fails to capture morphological nuances.
Character-Level (n-grams):
Method: Breaks text down into single characters or small groups of characters.(bigrams/trigrams).
Drawbacks: While it creates a truly open vocabulary, it results in much longer sequences. This reduces time and space efficiency and increases the distance over which the neural network must pass information, potentially degrading performance.

# Week 5: Chinese Legal Document RAG System - Ablation Study & Semantic Chunking

## Project Overview

This week, I built and optimized a Retrieval-Augmented Generation (RAG) system for Chinese legal documents, specifically targeting the Chinese Civil Code. The project consisted of two main experimental tasks:
- **Task 1**: Systematic ablation study to optimize RAG components for Chinese text processing
- **Task 2**: Comparison between fixed-size and semantic chunking strategies

**Dataset**: `lawbook_sample.pdf` - Chinese Civil Code (50 pages)  
**Test Queries**: 10 professional legal questions  
**Evaluation Metrics**: Context Relevance (CR), Answer Relevance (AR), Faithfulness (F), Overall Score

---

## Task 1: Chinese RAG Optimization - Ablation Study

### Experimental Design

My experimental approach followed a two-phase strategy:

**Phase 1: Systematic Ablation Testing** (`quick_ablation.py`)  
I tested 7 configurations systematically using all 10 queries to identify which improvements mattered most. This comprehensive test isolated the impact of each component (embedding model, segmentation method, overlap, hybrid search) individually and in combination.

**Phase 2: Web Interface Validation** (`compare_ui_versions_web_task1.py`)  
After Phase 1 identified Test 5 as the winner, I built an interactive Streamlit interface to validate the optimal configuration through 3 independent runs, ensuring the results were statistically robust.

### Baseline Configuration

**Important Note**: The baseline I used was `pdf_rag_ui_ollama_baseline.py`, which already included two critical modifications for Chinese text:

1. ✅ **Chinese prompt template** - Tailored for legal document queries in Chinese
2. ✅ **Fixed tokenization issue** - Removed space-based character splitting that breaks Chinese text processing

**Without these two fixes, the system cannot properly tokenize or process Chinese characters.** These were my starting point, not improvements being tested.

The baseline configuration for comparison was:
- **Embedding**: `all-mpnet-base-v2` (multilingual, not Chinese-optimized)
- **Segmentation**: spaCy English tokenizer (suboptimal for Chinese sentence boundaries)
- **Chunking**: 10 sentences per chunk, **0 overlap**
- **Retrieval**: Vector search only (no hybrid keyword matching)

### Test Configurations

I designed 6 improvement tests, progressively adding optimizations:

1. **Test 1**: Chinese embedding only (`BAAI/bge-large-zh-v1.5`)
2. **Test 2**: Jieba segmentation only (proper Chinese sentence boundaries)
3. **Test 2.5**: Jieba + 2-sentence overlap
4. **Test 3**: Chinese embedding + Jieba
5. **Test 4**: Test 3 + Overlap
6. **Test 5 (ALL)**: Chinese embedding + Jieba + Overlap + Hybrid search 🏆

### Key Results

**Phase 1: Ablation Study Results** (from `ablation_summary_20251226_161907.csv`):

| Configuration | Context Relevance | Answer Relevance | Faithfulness | Overall | Δ% vs Baseline |
|---------------|-------------------|------------------|--------------|---------|-----------------|
| BASELINE | 0.625 | 0.760 | 0.767 | 0.718 | — |
| Test 1 (BGE only) | 0.611 | 0.808 | **0.838** | 0.752 | **+4.8%** |
| Test 2 (Jieba only) | **0.721** | 0.763 | 0.704 | 0.729 | **+1.6%** |
| Test 2.5 (Jieba+Overlap) | 0.683 | 0.793 | 0.808 | 0.761 | **+6.1%** |
| Test 3 (BGE+Jieba) | 0.644 | 0.781 | 0.857 | 0.761 | **+6.0%** |
| Test 4 (BGE+Jieba+Overlap) | 0.634 | 0.760 | **0.868** | 0.754 | **+5.1%** |
| **Test 5 (Winner)** 🏆 | 0.634 | **0.834** | 0.818 | **0.762** | **+6.2%** |

**Phase 2: Web Interface Validation** (Average of 3 independent runs):

| Metric | Baseline | Test 5 | Δ | Δ% |
|--------|----------|--------|-----|-----|
| Context Relevance | 0.625 | 0.634 | +0.009 | +1.5% |
| Answer Relevance | 0.797 | 0.811 | +0.014 | +1.8% |
| **Faithfulness** | 0.766 | **0.844** | **+0.078** | **+10.2%** |
| Overall | 0.729 | 0.763 | +0.034 | +4.7% |

### Critical Findings & Analysis

**1. Faithfulness: The Most Important Improvement**

Faithfulness measures whether the generated answer is grounded in retrieved context without hallucination. This is **the most critical metric for RAG systems** because it directly addresses the core problem RAG aims to solve: providing factually accurate, source-backed answers.

My ablation study revealed dramatic faithfulness improvements:
- **Chinese embedding alone (Test 1)**: +9.3% (0.767 → 0.838)
- **Adding overlap (Test 4)**: +13.2% (0.767 → 0.868) - highest single improvement!
- **Web validation average**: +10.2% for Test 5 configuration

**Why This Matters for Legal Applications**: For legal document queries, users must trust that answers are directly derived from authoritative sources. A 10%+ improvement in faithfulness means significantly fewer hallucinations and more reliable citations - crucial for professional legal applications where accuracy is paramount.

**2. Component-Level Impact Analysis**

**Chinese Embedding (BGE-large-zh-v1.5)**: +4.8% overall improvement
- Most consistent single component across all metrics
- Answer Relevance: +6.3% (0.760 → 0.808)
- Faithfulness: +9.3% (0.767 → 0.838)
- Specialized training on Chinese corpus enables better semantic understanding of legal terminology

**Jieba Segmentation**: +1.6% overall, but +15.4% context relevance!
- Context Relevance: +15.4% (0.625 → 0.721) - **massive retrieval improvement**
- Proper Chinese sentence boundaries create semantically coherent chunks
- However, without overlap, faithfulness drops (-8.2%), revealing that chunking boundaries cut important context

**2-Sentence Overlap**: +4.5% when added to Jieba
- Faithfulness: +14.8% (0.704 → 0.808) - **critical for context preservation**
- Maintains semantic continuity across chunk boundaries
- Prevents loss of cross-reference information common in legal documents

**Hybrid Search (BM25 + Vector)**: +0.8% final boost

- Answer Relevance: +9.7% (0.760 → 0.834)
- Keyword matching complements semantic search for definition-heavy queries
- Particularly effective when users query specific article numbers or legal terms

**3. Answer Relevance: A Less Reliable Metric in My Implementation**

My evaluation uses a simplified approach for answer relevance: measuring how many query terms appear in the generated answer. Through my experiments, I discovered this metric has significant limitations.

**Observed Trade-off**: When faithfulness increases, answer relevance sometimes decreases:
- **Test 1**: Faithfulness +9.3%, Answer Relevance +6.3% (both improve)
- **Test 4**: Faithfulness +13.2%, Answer Relevance ±0% (faithfulness up, AR flat)
- **Test 5**: Faithfulness +6.7%, Answer Relevance +9.7% (both improve with hybrid)

**Intuitive Explanation**: When my system generates more faithful answers by directly citing legal text, it naturally repeats fewer terms from the user's question. A faithful answer that quotes "Article 18 stipulates..." verbatim may not echo the exact phrasing of the query "What does Article 18 say about...", but it's objectively a better answer because it's grounded in the source.

**Conclusion**: I interpret answer relevance cautiously. A decrease doesn't necessarily indicate worse performance - it may reflect more faithful, citation-heavy responses. This is why I prioritize faithfulness as my north star metric.

**4. Statistical Robustness Analysis**

**Run-to-Run Consistency** (from 3 independent web validation runs):

*Baseline Variance* (3 runs):
- Context Relevance: 0.625 (0.0% variance) - Perfect consistency (deterministic retrieval)
- Answer Relevance: 0.764-0.820 (7.3% range) - Moderate LLM variance
- Faithfulness: 0.723-0.791 (9.4% range) - Typical LLM non-determinism
- Overall: 0.722-0.739 (2.3% range)

*Improved (Test 5) Variance*:
- Context Relevance: 0.634 (0.0% variance) - Perfect consistency
- Answer Relevance: 0.794-0.830 (4.5% range) - **Lower than baseline**
- Faithfulness: 0.835-0.854 (2.3% range) - **Much more stable than baseline**
- Overall: 0.754-0.769 (2.0% range)

**Key Insight**: My optimized configuration not only performs better on average but also shows **more consistent behavior** across runs. The reduced variance in faithfulness (9.4% → 2.3%) and answer relevance (7.3% → 4.5%) suggests that better retrieval quality reduces LLM uncertainty, leading to more predictable outputs - a critical property for production systems.

---

## Task 2: Semantic Chunking Comparison

### Experimental Design

**Hypothesis**: Variable-size chunks that respect semantic boundaries should outperform fixed-size chunks by maintaining topic coherence within each chunk.

I compared two chunking strategies using the same infrastructure:

**1. Fixed-size Baseline** (Task 1's optimal configuration):
- BGE-large-zh-v1.5 embedding
- Jieba segmentation
- 10 sentences per chunk, 2 overlap
- Hybrid search enabled

**2. Semantic Chunking** (my Task 2 innovation):
- Same embedding, segmentation, hybrid search as baseline
- **Variable-size chunks (7-10 sentences)** based on embedding similarity
- Splits when cosine similarity < 0.5 (detected topic boundary)
- **Only difference**: Chunking strategy (adaptive vs fixed)

### Results & Analysis

**Run-to-Run Variability** (2 runs on same 10 queries):

| Metric | Run 1 Δ% | Run 2 Δ% | Average Δ% |
|--------|----------|----------|------------|
| Context Relevance | **+7.9%** | +0.4% | **+4.2%** |
| Answer Relevance | +7.0% | **-4.0%** | +1.2% |
| Faithfulness | +0.1% | **-0.9%** | **-0.5%** |
| Overall | **+4.8%** | **-1.7%** | **+1.5%** |

**Critical Observation**: Unlike Task 1 (which showed consistent improvements), Task 2 results exhibit **high variance between runs** - Run 1 showed +4.8% improvement while Run 2 showed -1.7% degradation.

### Why Semantic Chunking Underperformed My Expectations

**1. Missing Faithfulness Boost - The Most Puzzling Result**

Unlike Task 1's improvements (which dramatically improved faithfulness), my semantic chunking shows:
- Run 1: Nearly unchanged (+0.1%)
- Run 2: Slight decrease (-0.9%)
- Average: **-0.5%** (compared to +10-13% in Task 1 improvements!)

**My Hypothesis**: Variable boundaries may inadvertently split important context that legal documents frequently reference across sections. While semantic chunking respects topic changes (e.g., splitting between "Marriage" and "Inheritance" chapters), legal documents often contain cross-references like "as defined in Article 5" or "pursuant to Chapter 2". My fixed overlap (2 sentences) consistently preserves these connections, while adaptive boundaries might cut them at topic transitions.

**2. Context Relevance Improvement Without Faithfulness Gain**

Semantic chunking **does** improve context relevance (+4.2% average), meaning it successfully retrieves more topically relevant chunks. However, this doesn't translate to better faithfulness - a surprising disconnect.

**My Explanation**: 
- Semantic chunking may retrieve chunks that are topically relevant but miss specific factual details
- Legal queries often need precise information (article numbers, dates, definitions) that span multiple semantic units
- Example: Query "What is the age requirement in Article 18?" might retrieve a chunk about "adulthood" (topically relevant) but miss the specific "18 years old" mentioned in an adjacent chunk
- Fixed chunks with overlap are more likely to capture these boundary-crossing details

**3. Answer Relevance Behavior Aligns with Task 1 Findings**

The mixed Answer Relevance results (+7.0% → -4.0%) actually reinforce my Task 1 observation about this metric's limitations. When semantic chunking improves faithfulness slightly (Run 1: +0.1%), Answer Relevance goes up (+7.0%). When faithfulness drops (Run 2: -0.9%), Answer Relevance drops more dramatically (-4.0%). This suggests my simplified term-matching metric is sensitive to how much the answer echoes the query versus citing source text.

---

## Implementation Highlights

**Modular Architecture** (`util/` library):
- `pdf_utils.py`: PDF text extraction
- `nlp_utils.py`: Jieba segmentation, overlapping chunking
- `embedings_utils.py`: Batch encoding with GPU acceleration
- `vector_search_utils.py`: Hybrid retrieval (BM25 + cosine similarity)
- `evaluation_utils.py`: Three-metric evaluation framework

**Reproducibility**:
- Command-line scripts: `quick_ablation.py`, `test_semantic_chunking.py`
- Interactive web interfaces: Streamlit apps for real-time comparison
- Automated result logging: JSON (detailed) + CSV (summary) exports

---

## Key Takeaways

1. **Faithfulness is my north star metric** - The +10% improvement in Task 1 directly addresses the hallucination problem that RAG aims to solve, far more valuable than marginal gains in other metrics.

2. **Chinese-specific optimizations matter** - BGE-zh-v1.5 embedding + Jieba segmentation together provide +6% improvement over multilingual baselines, proving that language-specific tools are worth the implementation effort.

3. **Overlap is critical for legal documents** - Even 2 sentences of overlap dramatically improves faithfulness (+14.8%), likely because legal text frequently contains cross-references that would otherwise be split at chunk boundaries.

4. **Simple metrics need careful interpretation** - My simplified answer relevance metric (term matching) shows an inverse relationship with faithfulness, revealing the limitations of naive evaluation approaches. When faithfulness goes up, answer relevance sometimes goes down - but the answer is objectively better.

5. **Adaptive techniques need extensive validation** - Semantic chunking's high variance (Run 1: +4.8%, Run 2: -1.7%) highlights the importance of testing on diverse query types before production deployment. What works for conceptual queries may fail for fact-finding queries.

6. **Statistical rigor requires multiple runs** - My 3-run web validation caught variance that single-run experiments would miss. The reduced variance in my optimized configuration (faithfulness: 9.4% → 2.3%) is as important as the mean improvement.
